t=int(input())
for _ in range(t):
    a,b,c,d=map(int,input().split())
    h=max(a,b)
    g=max(c,d)
    nums=[a,b,c,d]
    nums.sort()
    o=h+g
    p=nums[-1]+nums[-2]
    if o>=p:
        print("YES")
    else:
        print("NO")
    