import math 
 
t=int(input())
for _ in range(t):
    a=int(input())
    nums=list(map(int,input().split()))
    h=sum(nums)
    g=(math.sqrt(h))
    if g.is_integer():
        print("YES")
    else:
        print("NO")